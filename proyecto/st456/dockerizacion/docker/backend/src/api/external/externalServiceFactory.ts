import { ExternalService } from './services/externalService';
import { ServiceConfig } from './serviceConfig';

export class ExternalServiceFactory {
    public createService(serviceConfig: ServiceConfig): ExternalService {
        // Implementación
    }
}